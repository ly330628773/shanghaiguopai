const myRequest = {

	request() {
		console.warn("ojnkkk", top);
		return true;
		/* var requestTask = uni.request({
			url: 'https://pai.52car.xyz/api/index', //仅为示例，并非真实接口地址。
			success: (result) => {
				console.log(result);
			}
		}); */
	}
};
export default {
	myRequest
};
